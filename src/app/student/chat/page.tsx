'use client';
import { FormEvent, useState } from 'react';
import Link from 'next/link';
import { BookOpenCheck, MessageSquareText, Send, ShieldCheck, Sparkles } from 'lucide-react';
import { apiFetch } from '@/lib/auth/apiFetch';
import SourcePagePreview from '@/components/SourcePagePreview';
export default function StudentChatPage() {
 const [question,setQuestion]=useState(''); const [answer,setAnswer]=useState<any>(null); const [error,setError]=useState(''); const [busy,setBusy]=useState(false);
 const ask=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{const res=await apiFetch('/api/student/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question})});const body=await res.json();if(!res.ok)throw new Error(body.error);setAnswer(body);}catch(cause:any){setError(cause.message)}finally{setBusy(false)}};
 const answerPoints=answer ? (Array.isArray(answer.answer) ? answer.answer : [answer.answer]) : [];
 return <main className="min-h-screen bg-[#f7f8fb] px-4 py-8 sm:px-6"><div className="mx-auto max-w-5xl"><Link href="/student" className="text-sm font-semibold text-slate-600 hover:text-slate-950">← Back to student portal</Link><section className="mt-5 overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 bg-gradient-to-br from-slate-950 to-slate-800 px-6 py-7 text-white"><p className="flex items-center gap-2 text-xs font-bold tracking-[0.16em] text-emerald-300"><ShieldCheck className="w-4" />PRIVATE, GROUNDED STUDY CHAT</p><h1 className="mt-3 text-3xl font-bold tracking-tight">Ask the notes you are assigned</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-300">Every answer is taken only from material linked to rooms you joined. Each answer includes the original PDF page as proof.</p></div><div className="p-5 sm:p-7"><form onSubmit={ask} className="rounded-2xl border border-slate-200 bg-slate-50 p-2 shadow-inner sm:flex sm:items-center"><input value={question} onChange={e=>setQuestion(e.target.value)} className="min-w-0 flex-1 bg-transparent px-3 py-3 text-sm outline-none" placeholder="For example: What is the role of the RIP periodic timer?"/><button disabled={busy||!question.trim()} className="btn-primary flex w-full items-center justify-center gap-2 px-5 py-3 sm:w-auto">{busy ? 'Finding evidence…' : 'Ask notes'}<Send className="w-4" /></button></form>{error&&<p className="mt-4 rounded-xl border border-rose-200 bg-rose-50 p-4 text-sm text-rose-700">{error}</p>}{answer&&<div className="mt-7 space-y-6"><section className="rounded-2xl border border-indigo-100 bg-indigo-50/70 p-5"><p className="flex items-center gap-2 text-xs font-bold uppercase tracking-wider text-indigo-700"><Sparkles className="w-4" />Grounded answer</p><ol className="mt-4 space-y-3">{answerPoints.map((point:string,index:number)=><li key={index} className="flex gap-3 text-sm leading-7 text-slate-800"><span className="flex h-6 w-6 shrink-0 items-center justify-center rounded-full bg-indigo-600 text-xs font-bold text-white">{index+1}</span><span>{point}</span></li>)}</ol></section><div><div className="mb-3 flex items-center gap-2 text-sm font-bold text-slate-800"><BookOpenCheck className="w-5 text-emerald-700"/>Open the exact source page</div>{answer.citations.map((citation:any,index:number)=><article key={`${citation.documentId}-${citation.pageNumber}-${index}`} className="mb-4 rounded-2xl border border-emerald-200 bg-white p-4 shadow-sm"><div className="flex items-start gap-2 text-sm font-bold text-emerald-950"><MessageSquareText className="mt-0.5 w-4 shrink-0 text-emerald-700"/><div>{citation.documentTitle}<span className="mx-2 text-emerald-300">·</span><span className="text-emerald-700">{citation.sourceLabel}</span>{citation.sectionTitle&&<p className="mt-1 text-xs font-medium text-slate-500">{citation.sectionTitle}</p>}</div></div><SourcePagePreview citation={citation} /></article>)}</div></div>}</div></section></div></main>;
}
