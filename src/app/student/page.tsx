'use client';
import { FormEvent, useState } from 'react';
import Link from 'next/link';
import { ArrowRight, MessageSquareText, ShieldCheck } from 'lucide-react';
import { useRouter } from 'next/navigation';
export default function StudentPortalPage() { const [code,setCode]=useState(''); const router=useRouter(); const submit=(e:FormEvent)=>{e.preventDefault(); if(/^[A-Z0-9]{6}$/i.test(code)) router.push(`/exam/${code.toUpperCase()}`);}; return <main className="min-h-screen bg-slate-50 p-5"><div className="mx-auto max-w-xl rounded-2xl border bg-white p-8 shadow-sm"><p className="flex gap-2 text-xs font-mono font-bold text-emerald-700"><ShieldCheck className="w-4"/>STUDENT WORKSPACE</p><h1 className="mt-2 text-3xl font-extrabold">Join your faculty room</h1><p className="mt-2 text-sm text-slate-500">Use a room code shared by your professor. No demo rooms or sample assessments are shown here.</p><form onSubmit={submit} className="mt-6 flex gap-2"><input value={code} onChange={e=>setCode(e.target.value.toUpperCase().slice(0,6))} className="flex-1 rounded-xl border px-4 py-3 font-mono uppercase" placeholder="ROOM CODE"/><button className="btn-primary px-4"><ArrowRight className="w-4"/></button></form><div className="mt-6 grid gap-3 sm:grid-cols-2"><Link href="/student/chat" className="flex items-center gap-2 rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm font-bold text-emerald-900"><MessageSquareText className="w-5"/>Ask assigned notes</Link><Link href="/student/history" className="rounded-xl border bg-white p-4 text-sm font-bold">Past quizzes →</Link></div></div></main>; }
